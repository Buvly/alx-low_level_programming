#include <stdio.h>
#include "main.h"

/**
 * _abs - function prints the absolute value of a numberi.
 * @n: is a single input number
 * Return: absolute value of an number
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

